import { DrawerStartManager } from './drawer-start-manager';

describe('DrawerStartManager', () => {
  it('should create an instance', () => {
    expect(new DrawerStartManager()).toBeTruthy();
  });
});
